<% if activity.attachment.image? %>
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-one-third">
      <%= render "documents/document_preview",
               document: activity.attachment,
               dimensions: "100x100",
               class_name: "product-image" %>
    </div>
  </div>
<% else %>
  <%= link_to "View document (opens in new tab)", activity.attachment, class: "psd-block-link", target: "_blank", rel: 'noopener' %>
<% end %>
