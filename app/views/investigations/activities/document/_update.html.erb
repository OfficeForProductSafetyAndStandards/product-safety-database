<% if activity.new_description %>
  <p><%= markdown simple_format(activity.new_description) %></p>
<% end %>

<% if activity.document.image? %>
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-one-third">
      <%= render "documents/document_preview",
               document: activity.document,
               dimensions: "100x100",
               class_name: "product-image" %>
    </div>
  </div>
<% else %>
  <%= link_to "View document (opens in new tab)", activity.document, class: "psd-block-link", target: "_blank", rel: 'noopener' %>
<% end %>
