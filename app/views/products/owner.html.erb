<%= page_title "Contact the product record owner - #{@product.name}" %>

<% content_for :after_header do %>
  <%= link_to "Back", @product, class: "govuk-back-link" %>
<% end %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= render "owner_contact_details", product: @product %>
  </div>
</div>
