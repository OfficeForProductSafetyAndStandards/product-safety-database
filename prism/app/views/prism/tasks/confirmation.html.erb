<% content_for :page_title, "Confirmation" %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= govuk_panel(title_text: "Your product risk assessment is complete") %>
    <p class="govuk-body">You have successfully added the risk assessment to <strong><%= sanitize(@prism_risk_assessment.product_name) %></strong>.</p>
    <h2 class="govuk-heading-m">Next steps</h2>
    <p class="govuk-body"><a href="<%= view_submitted_assessment_risk_assessment_tasks_path(@prism_risk_assessment) %>" class="govuk-link">View</a> or <a href="<%=  main_app.add_to_case_prism_risk_assessments_path(prism_risk_assessment_id: @prism_risk_assessment.id) %>" class="govuk-link">assign the assessment to the product case</a>.</p>
  </div>
</div>
