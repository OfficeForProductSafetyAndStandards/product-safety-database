<% page_title = "Record #{@test.pretty_name}" %>
<%= page_title page_title, errors: @test.errors.any? %>

<% content_for :after_header do %>
  <%= govukBackLink text: "Back",
      href: new_investigation_supporting_information_path(@investigation) %>
<% end %>

<%= form_with model: @test, scope: :test, local: true, url: create_draft_investigation_tests_path(@investigation.pretty_id), method: :put do |form| %>
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds-from-desktop">

      <%= render "form_components/govuk_error_summary", form: form %>

      <span class="govuk-caption-l"><%= @investigation.pretty_description %></span>
      <h1 class="govuk-heading-l"><%= page_title %></h1>

      <%= render "form", form: form, test: @test, investigation: @investigation, allow_product_linking: true %>

      <%= govukButton text: "Continue" %>
    </div>
  </div>
<% end %>
