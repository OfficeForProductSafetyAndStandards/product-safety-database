<details class="govuk-details">
  <summary class="govuk-details__summary">
    <span class="govuk-details__summary-text">
      How we measure most active teams (active users)
    </span>
  </summary>
  <div class="govuk-details__text">
    <p>
      We determine a user to be active if they have used PSD in the last 3 months.
    </p>
    <p>
      We then count the number of active users in each team and rank them.
    </p>
  </div>
</details>

<%=
  govuk_table do |table|
    table.with_head do |head|
      head.with_row do |row|
        row.with_cell(text: "Team name")
        row.with_cell(text: "Active users")
      end
    end

    table.with_body do |body|
      @by_activity.decorate.each do |team|
        body.with_row do |row|
          row.with_cell(text: team.name)
          row.with_cell(text: team.users_count)
        end
      end
    end
  end
%>
