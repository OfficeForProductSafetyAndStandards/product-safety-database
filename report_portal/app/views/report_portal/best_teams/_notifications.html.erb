<%=
  govuk_table do |table|
    table.with_head do |head|
      head.with_row do |row|
        row.with_cell(text: "Team name")
        row.with_cell(text: "Notifications")
      end
    end

    table.with_body do |body|
      @by_notifications.decorate.each do |team|
        body.with_row do |row|
          row.with_cell(text: team.name)
          row.with_cell(text: team.notifications_count)
        end
      end
    end
  end
%>

<details class="govuk-details">
  <summary class="govuk-details__summary">
    <span class="govuk-details__summary-text">
      How we measure best teams (notifications)
    </span>
  </summary>
  <div class="govuk-details__text">
    <p>
      We count the number of notifications created by each team in the last 12 months.
    </p>
    <p>
      We then rank the teams by the number of notifications they have created, with the team that has created the most notifications ranked first.
    </p>
  </div>
</details>
