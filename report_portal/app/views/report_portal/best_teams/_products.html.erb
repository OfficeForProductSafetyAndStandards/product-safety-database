<details class="govuk-details">
  <summary class="govuk-details__summary">
    <span class="govuk-details__summary-text">
      How we measure most active teams (products)
    </span>
  </summary>
  <div class="govuk-details__text">
    <p>
      We count the number of products created by each team, and then rank them in order of most to least.
    </p>
  </div>
</details>

<%=
  govuk_table do |table|
    table.with_head do |head|
      head.with_row do |row|
        row.with_cell(text: "Team name")
        row.with_cell(text: "Products")
      end
    end

    table.with_body do |body|
      @by_products.decorate.each do |team|
        body.with_row do |row|
          row.with_cell(text: team.name)
          row.with_cell(text: team.products_count)
        end
      end
    end
  end
%>
