<%=
  govuk_table do |table|
    table.with_head do |head|
      head.with_row do |row|
        row.with_cell(text: "Team name")
        row.with_cell(text: "Users")
      end
    end

    table.with_body do |body|
      @by_users.decorate.each do |team|
        body.with_row do |row|
          row.with_cell(text: team.name)
          row.with_cell(text: team.users_count)
        end
      end
    end
  end
%>

<details class="govuk-details">
  <summary class="govuk-details__summary">
    <span class="govuk-details__summary-text">
      How we measure least active teams (active users)
    </span>
  </summary>
  <div class="govuk-details__text">
    <p>
      We determine an active user to be one that has used PSD in the last 3 months
    </p>
    <p>
      We look for all teams that have had no acitve users in the last 3 months. The list is ordered alphabetically by team name.
    </p>
  </div>
</details>
